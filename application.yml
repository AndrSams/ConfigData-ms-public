info: 
  component: ${spring.application.name} 

    
---
spring:
  profiles: localdocker
eureka:
  client:
    serviceUrl:
      defaultZone: http://DOCKER:8010/eureka/

graphite:
  host: DOCKER
  port: 2003
  # no access to graphite
  enabled: false

spring:
  zipkin:
    baseUrl: http://DOCKER:8070

---
spring:
  profiles: localhost
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka/
#      defaultZone: http://localhost:8011/eureka/,http://localhost:8012/eureka/,http://localhost:8013/eureka/

graphite:
  host: localhost
  port: 2003
  # no access to graphite
  enabled: false

spring:
  zipkin:
    baseUrl: http://localhost:8070
